# Bond API reference (integrator docs)

These files were copied from the Bond **squad-c** documentation for convenience.

- **`API_CONSUMER_PROMPTS.md`** — How external apps should use the public API (BFF, no exposed keys).
- **`PUBLIC_APIS_FOR_AGENTS.md`** — Swagger tag → controller map inside Bond’s `apiv2` repo.

**This portal does not import Bond code.** For request/response shapes and URLs, use the **hosted** spec:

- [Squad C Swagger UI](https://public.api.squad-c.bondsports.co/public-api/)
- [bond-public-api.json](https://public.api.squad-c.bondsports.co/public-api/bond-public-api.json)

Refresh copies here when those docs change on squad-c.
